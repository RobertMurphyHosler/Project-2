/*
//description
//camera
view_enabled = true;
view_visible[0] = true;

var width = 480; height = 270; scale = 2;

var cam = camera_create_view(0, 0, width, height, 0, obj_Player, -1, -1, width/2, height/2);
view_set_camera[0, cam];

window_set_size(width*scale, height*scale);
surface_resize(application



